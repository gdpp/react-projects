import { Link, Outlet } from 'react-router-dom';

const Portfolio = () => {
    return (
        <div>
            <h1>Portfolio</h1>
            <Outlet />
        </div>
    );
};

export default Portfolio;
